#include <stdio.h>

int main()
{
	int a,b;
	char c1, c2;
	float x, y;
//	scanf("a=%db=%d",&a,&b);
//	printf("a is %d, b is %d",a,b);
	scanf("%f%e",&x,&y);
	scanf("%c%c",&c1,&c2);
printf("\n\nx is %f, y is %e, c1 is %c, c2 is %c", x, y, c1, c2);
	return 0;
}
